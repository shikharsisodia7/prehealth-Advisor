You are working inside an already existing Health Professions Planner and advising application. Do not create a new project, replacement prototype, disconnected demo, mock implementation, or separate proof of concept. First inspect the entire existing repository, including the frontend, backend, API routes, database schema, migrations, scraping scripts, program datasets, profession mappings, filtering logic, export logic, environment configuration, tests, and deployment configuration. Understand how the current application works before changing anything. Preserve the existing visual design and the parts of the user experience that already work well, especially the ability to select a profession, filter programs by state, select one or multiple programs, view prerequisites, copy results, download an Excel file, and open the official program website. This is a repair and completion task for the existing production application, not a redesign.

The application currently has several connected data-quality and functionality problems that must all be fixed. Some health professions show incomplete program lists, some professions show only one or a few programs even though many valid programs exist, prerequisite information is frequently missing and replaced by “Prerequisites are still being verified,” prerequisite information is not reliably included when multiple programs are selected, direct source links can disappear in multi-program results, and downloaded Excel files do not consistently contain prerequisite information. Medical school programs also need to be separated into MD and DO options, while still allowing users to select both. Do not fix only one visible symptom. Trace each issue through the database, API, scraping pipeline, normalization logic, filtering logic, frontend rendering, and export functionality, then correct the underlying causes.

Begin by performing a full technical audit of the existing application. Identify where programs come from, how professions are categorized, how state filters are applied, whether results are truncated by API limits, database limits, pagination, frontend slicing, deduplication, incorrect profession aliases, incorrect degree mappings, stale cached data, or failed scraping jobs. Inspect all code paths responsible for loading and displaying programs. Check for hidden limits such as `.limit()`, `slice()`, fixed page sizes, incomplete pagination loops, missing cursor handling, API response caps, query conditions that accidentally exclude programs, inconsistent profession names, and frontend state bugs. Do not assume that missing programs are only a scraping problem. Determine whether the records are absent from the database or present but not being returned or displayed.

Create a reliable program inventory for every profession currently supported by the application. Compare the application’s stored program counts with authoritative public sources such as official accrediting organizations, centralized application services, professional associations, and official university program pages. Prefer authoritative structured sources where available instead of relying entirely on general web search. Each program record must contain a stable internal identifier, profession, institution name, official program name, degree type, state, city when available, official program URL, admissions or prerequisite URL when different, accreditation or source reference when available, active status, and last-verified date. Normalize institution and program names while preserving their official display names. Remove true duplicates without merging distinct campuses, degree tracks, or program types. Do not invent programs, URLs, prerequisite courses, school information, or accreditation details. Do not use placeholder data.

Specifically investigate the professions identified during testing. Dental programs are currently incomplete and must include the full valid set of dental schools supported by authoritative sources. Master’s-entry nursing programs must not show only Vanderbilt if other eligible programs exist. Accelerated bachelor’s nursing programs must not show only Samuel Merritt if other eligible programs exist. Medical, physician assistant, physical therapy, pharmacy, podiatry, and every other existing profession must also be audited rather than assumed correct merely because one test appeared successful. Program lists must remain correct when “All States” is selected and must narrow accurately when an individual state is selected. A profession with zero programs must display a truthful empty state and diagnostic information internally, not silently fail or show unrelated programs.

Repair and strengthen the prerequisite collection pipeline. The existing scraper is inconsistent across university websites, so implement a layered extraction system rather than depending on a single technique. The pipeline should first attempt a lightweight static HTTP request for ordinary HTML pages. It should then use a browser-based fallback such as Playwright or Selenium for JavaScript-rendered pages, delayed content, client-side routing, expandable admissions sections, and pages that require browser interaction. Prefer Playwright if it integrates more naturally with the existing stack, but use Selenium when it is technically more appropriate or already supported. The Yale comment about a custom Python Selenium scraper should be treated as evidence that browser automation can be useful, not as a requirement to blindly reuse a Canvas-specific approach. This application is scraping public admissions pages across many unrelated institutions, so the implementation must support static HTML, JavaScript-rendered content, tables, tabs, accordions, linked admissions pages, PDFs, redirects, and multiple page layouts.

For each official program website, identify likely admissions and prerequisite sources by following relevant internal links and examining page titles, headings, anchor text, metadata, and URL patterns containing terms such as prerequisites, prerequisite courses, admissions requirements, admission criteria, academic requirements, required coursework, application requirements, eligibility, program requirements, prospective students, how to apply, and admissions. Search within the official program domain first. Do not scrape random third-party summary sites when an official source is available. Support prerequisite information stored in PDFs and official handbooks. When a PDF contains the relevant information, extract and preserve the exact source page URL and, where practical, the page number or section heading.

Convert collected prerequisite information into a consistent structured schema. Each prerequisite should support fields such as subject or course category, course name when provided, number of semesters or quarters, credit hours, lab requirement, minimum grade, recency or expiration requirement, online-course policy, AP or IB credit policy, community-college credit policy, substitutions, recommended versus required status, and notes. Also capture broader admissions requirements when they directly affect prerequisite planning, including bachelor’s-degree requirements, standardized tests, minimum GPA, observation hours, clinical experience, certification requirements, and application-service requirements. Keep prerequisite courses distinct from recommendations and general admissions criteria so the interface does not mislabel recommended coursework as mandatory.

Do not use an LLM to fabricate missing fields. An LLM may be used only to normalize or structure text that has already been extracted from an official source, and the structured result must remain traceable to that source. Preserve the raw extracted text or a source excerpt internally for auditing. Add confidence or verification metadata to every prerequisite record. At minimum, support statuses such as verified, extracted-needs-review, unavailable-on-official-site, blocked, and extraction-failed. Replace the vague default “Prerequisites are still being verified” behavior with a more accurate status based on what actually happened. A program must not be marked verified unless its source was successfully retrieved and its prerequisite information was confirmed. A failed scrape must not overwrite previously verified data with empty values.

Implement retries, timeouts, redirects, browser cleanup, rate limiting, domain-aware throttling, caching, and error isolation so one failed institution does not stop the full ingestion job. Use a reasonable user agent and access only public information. Avoid authentication bypasses, CAPTCHAs, private portals, or restricted content. Log failures with the program identifier, URL, failure category, HTTP status where applicable, extraction stage, and timestamp. Make the scraper resumable so failed records can be retried without rerunning every successfully verified program. Add a command or admin workflow for refreshing one program, one profession, failed programs only, stale programs only, or the entire dataset.

Store source provenance for every program and prerequisite result. The application should retain the official source URL, page title when available, retrieval timestamp, verification timestamp, extraction method, and status. The user-facing result must show the official source link for every selected program, even when multiple programs are selected. When prerequisites are unavailable, the user should still be able to open the most relevant official admissions page. Do not show a generic institution homepage when a more specific admissions or prerequisite page has been identified.

Fix the single-program and multi-program result displays. Selecting one program must show that program’s structured prerequisite courses, additional admissions notes, verification status, last-verified date, and official source link. Selecting multiple programs must render a separate clearly labeled result section, card, table group, or expandable panel for every selected program. The data from different programs must never be merged together in a way that makes it unclear which prerequisite belongs to which school. Each program result must keep its own institution name, program name, degree type, location, prerequisite list, notes, source URL, and verification status. The interface must handle large selections without losing records, hiding source links, duplicating results, or becoming unusable. Add loading, partial-failure, empty, and error states that explain what happened without exposing internal stack traces.

Fix the copy-results functionality. Copying results for one or multiple programs must include each program’s institution, official program name, degree type, location, structured prerequisites, prerequisite details, additional admissions requirements where applicable, verification status, last-verified date, and source URL. The copied output must remain readable when many programs are selected. Ensure the copy operation uses the complete data model rather than copying only the visible summary text.

Fix the Excel export functionality completely. The downloaded workbook must include the selected programs and their prerequisite information. Use a normalized format that is useful for advising and comparison. Prefer one row per prerequisite requirement, with columns for profession, institution, program, degree type, state, city, prerequisite subject, course requirement, semesters or credits, lab requirement, minimum grade, prerequisite notes, broader admissions notes, verification status, last verified, and official source URL. Include a separate program-summary sheet when useful. Ensure special characters, long notes, URLs, blank optional values, and large exports are handled correctly. The export must contain the same underlying data shown in the interface. Do not export “still being verified” as a substitute when verified prerequisite records exist in the database.

Add the requested medical-school degree filter. Medical school users must be able to select MD, DO, or both. Store MD and DO as explicit degree-type metadata rather than inferring them only from school names at render time. The default may remain both so existing behavior is not unnecessarily disrupted. Ensure this filter works with state filtering, program selection, prerequisite display, copied results, and Excel exports. Do not incorrectly categorize dental, podiatry, or other doctoral programs as MD or DO.

Add support for Certified Anesthesiologist Assistant and Pathologists’ Assistant as distinct profession categories after the existing critical issues are working. Do not place them in a generic master’s category. Populate their valid programs from authoritative sources, assign correct degree types, collect official program and admissions URLs, and run them through the same prerequisite extraction, verification, display, copy, and export pipeline as all other professions. The profession labels should use accepted names and should be consistent across the database, API, frontend, and exports.

Preserve backward compatibility wherever reasonable. Reuse the existing architecture and components when they are sound. If the current data model cannot support structured prerequisites, source provenance, degree types, or verification metadata, create proper database migrations rather than storing everything in an unstructured text field. Migrations must be safe for the existing data. Backfill existing records where possible, preserve previously valid data, and prevent duplicate records. Do not delete the existing database or reset production data to make the implementation easier. Update seed scripts and ingestion scripts so future runs continue to produce correct data.

Add data validation at ingestion and API boundaries. Reject or quarantine malformed records such as missing institution names, unsupported profession values, invalid URLs, impossible states, duplicated stable identifiers, prerequisites without a parent program, or records marked verified without a source. Add appropriate database constraints and indexes where safe. Make program and prerequisite APIs return deterministic, complete, correctly paginated results. Avoid N+1 queries and excessive browser launches. Cache stable source results while allowing forced refreshes.

Create automated tests for the problems described in the feedback. Add unit tests for profession normalization, MD and DO filtering, state filtering, deduplication, prerequisite parsing, verification status logic, and export row generation. Add integration tests for API completeness and database relationships. Add end-to-end tests with Playwright for selecting a profession, selecting all states, selecting one program, selecting multiple programs, viewing prerequisites, opening source links, copying results, downloading an Excel file, and using the MD, DO, and both filters. Include regression tests for dental programs, master’s-entry nursing, accelerated bachelor’s nursing, Ohio University PA, Emory PT, Vanderbilt master’s-entry nursing, Samuel Merritt accelerated nursing, and representative medical, pharmacy, podiatry, PA, PT, and dental programs. Where live university websites would make tests unstable, use saved fixtures based on real page structures and add a separate controlled live verification script.

Create an internal data-audit report or command that summarizes, by profession, the expected program count from the selected authoritative source, the number stored in the application, the number with verified prerequisites, the number needing review, the number with failed extraction, and the number missing official source URLs. Use this report to find incomplete professions before declaring the work complete. Counts may change over time, so store the source and audit date rather than hardcoding unsupported numbers into the interface.

Review environment variables, deployment configuration, browser dependencies, serverless runtime limitations, execution timeouts, and scheduled ingestion behavior. Browser scraping may not run correctly in every serverless environment, so place long-running ingestion in an appropriate worker, background job, administrative script, or scheduled process already compatible with the application’s infrastructure. Do not perform expensive live scraping during a normal user page request. User requests should read previously collected data from the database and return quickly. Provide a clear refresh mechanism for administrators or scheduled jobs.

Do not break the current working UI while repairing the data. Do not replace real records with mocked examples. Do not hide missing information by hardcoding prerequisite lists into frontend components. Do not simply change the message text while leaving the underlying data absent. Do not add a scraper that only works for one university. Do not mark the task complete because a few selected programs work. Do not return after writing a plan, explaining what should be done, or making only partial edits.

Work autonomously through the repository and implement the full solution. Use the available coding, testing, browser, database, and debugging tools. When you encounter an error, investigate and fix it rather than stopping to report the first failure. Continue iterating until the application builds successfully, database migrations run safely, linting and type checks pass, automated tests pass, critical end-to-end flows pass, exports contain prerequisite data, multi-program results retain individual source links, incomplete profession lists have been corrected, and the prerequisite pipeline works across representative static HTML, JavaScript-rendered pages, and PDF-based admissions sources. Do not come back merely saying that the work is large, that scraping is difficult, that more research is needed, or that the user should manually finish it. Complete every part that can be completed from the repository and public authoritative sources.

Before finishing, manually verify the application using the same behaviors described in the feedback. Check medical schools with MD only, DO only, and both. Check dental programs and confirm the list is not artificially limited. Check master’s-entry nursing and accelerated bachelor’s nursing and confirm that more than a single hardcoded program appears when authoritative data supports more. Check PA, PT, pharmacy, podiatry, and other current professions. Open Ohio University PA and Emory PT and confirm that the app shows available prerequisite information from the official admissions sources. Select at least two programs simultaneously and confirm that each has its own prerequisite list and official source link. Copy the multi-program results and inspect the copied text. Download the Excel file and inspect the workbook contents rather than merely confirming that a file was generated. Confirm that prerequisite rows, program names, verification statuses, and URLs are actually present.

Do not claim success unless you have evidence. At the end, provide a precise completion report containing the root causes you identified, the files and systems changed, database migrations added, scraping strategies implemented, authoritative sources used, program-count audit results by profession, prerequisite coverage results, tests executed, build and type-check results, examples manually verified, remaining extraction failures with exact reasons, and commands needed to run or refresh the scraper in development and production. Any genuinely blocked program must be listed individually with its URL, error category, and the fallback behavior implemented. The completion report must distinguish fully verified work from anything still requiring human review.

The final acceptance criteria are that the existing Health Professions Planner continues to provide its current selection workflow, program lists are complete and correctly filtered according to authoritative available data, MD and DO can be selected separately or together, prerequisite information is structured and displayed whenever it is publicly available, every program retains its own official source link, multiple selected programs display independently, copy results include prerequisites and sources, Excel exports contain complete program and prerequisite data, failed extraction states are accurate and traceable, the new professions are supported, and the application passes its build, type-check, migration, automated-test, and end-to-end verification processes. Do not stop until these acceptance criteria have been tested and either satisfied or documented as a specific externally blocked case with a working fallback.
